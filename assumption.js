function Assumption(ArrayOfOutcomes, TruthDecision, ArrayOfConditions, Domains){
	// Note that the variable names must be consistent when specifying the assumtion
	
	// TruthDecision = true, false, possibly true, true under unknown conditions, 

	// Array of conditions is empty if there are zero conditions
	
	// Domains
		// Natural
		// Integer
		// Real
		// Rational
		// Irrational
		// Complex
		// Assumption
		// Functions
		// Equation/inequality
		// Positive/negative
	// Domains=[];
	// Domains[0]={};
	// Domains[0].Variable="x";
	// Domains[0].Domains=["N", "R", "C"];
	
	


}





Assumption.prototype.Substitute=function(){
	
	
};


//list of inititating values
// x=y true if y=x
// x+b=y+b true if x=y
// x<y false if x>y